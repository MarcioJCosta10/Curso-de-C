#include <stdio.h>
#include <stdlib.h>
#include "ListaSequencial.h"//Importando minha biblioteca

            /*Implementando uma "Lista Sequncial Estática"
            "ListaSequncial.h":definir
            - Os protótipos das funções
            - O tipo de dado armazenado na lista
            - O ponteiro "lista"
            - Tamanho do vetor usado na lista

            "ListaSequencial.c": definir
            - O tipo de dados "lista"
            - Implementar suas funções.*/

int main()
{       // Aqui é como as função serão chamadas no programa principal
        // Para usar a minha estrutura Lista no programa principal vou criar um ponteiro no caso (*li) para o meu tipo Lista(que é a minha struct)

    //Lista *li;
    //li = cria_lista();
    //libera_lista(li);
    //int x = tamanho_lista(li);
    //int x = lista_cheia(li);
    //if(lista_cheia(li))
    //int x = lista_vazia(Lista * li);
    //if (lista_vazia(li))
    //int x = insere_lista_final(li,dados_aluno); // x é o meu retorno passo li que é o Lista*, dados do aluno que é uma estrutura que contem nome as 3 notas e matricula
    //int x = insere_lista_inicio(li,dados_aluno);// x é o código de erro + lista (li) + dados do aluno
    //int x = insere_lista_ordenada(li,dados_aluno);// sempre do mesmo jeitão de charmar ela
    //int x = remove_lista_final(li);
    //int x = remove_lista_inicio(li);
    //int x = remove lista(li, matricula_aluno);
    //int x = consulta_lista_pos(li, posicao, &dados_aluno); // Chamo a função, Passo minha lista, passo a posição que eu quero e passo endereço da estrutra que vou guardar os dados que eu quero recuperar da lista
    //int x = consulta_lista_mat(li, posicao, &dados_aluno); // Chamo a função, passo minha lista, passo a posição que no caso é a matricula, e passo endereço da estrutra que vou guardar os dados que eu quero recuperar da lista





    return 0;
}
