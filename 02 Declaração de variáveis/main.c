#include <stdio.h>
#include <stdlib.h>

int main()
{
    //Fomato declaração das variaveis: Tipo e nome;
    //Tipo:
    char;
    int;
    float;
    double;

    //Nome:
    //a, b, c, d,...
    //A, B, C, D,...
    //0, 1, 2,..., 9
    //x
    //soma
    //_soma
    //soma1
    //C é case sensitive pois diferencia maiusculo e minusculo:
    //SOMA
    //Soma
    //somA

    //Regras: Nãoo posso usar para declarar variaveis
    //a, b, x, y,...
    //!, *, &, $,...
    //Palavras reservadas(Palavras que existem em C com algun significado Ex: Int )

    char letra = 'a';
    char nro = 10;
    int nro = 10;
    float n = 5.25;
    double d = 5.0e3; //(notação cientifica);

    int nro1 = 10;
    int nro2;
    int nro3;
    int nro1 = 10; //nro2, nro3;

    return 0;
}
